import { useEffect, useRef } from 'react';
import { useNavigate, useLocation } from 'react-router-dom';

// This component is now disabled to prevent unwanted redirects
export const RedirectToOCR = () => {
  // No redirects will happen
  return null;
};
